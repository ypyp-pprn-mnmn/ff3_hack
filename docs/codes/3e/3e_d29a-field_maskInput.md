

# $3e:d29a field_maskInput


### code:
```js
{
	if ( ($20 & 3) != 0) { //beq d2a2
		x = 3;
	}
$d2a2:
	$81 = x;
	if (($20 & #c) != 0) { //beq $d2af
		$81 = x | #c;
	}
$d2af:
	x = $21 = a = (($20 ^ $21) & $81) ^ $21;
	if ((a & #10) != 0) { //beq $d2ce
		if (($20 & #10) != 0) { // beq d2c8
			$23++;
		}
		$21 ^= #10;
	}
$d2ce:
	a = x & #20;
	if (a != 0) { //beq $d2e1
		if (($20 & #20) != 0) { //beq d2db
			$22++;
		}
$d2db:
		$21 ^= #20;
	}
$d2e1:
	a = x & #40;
	if (a != 0) { //beq $d2f4
		if (($20 & #40) != 0) { //beq d2ee
			$25++;
		}
		$21 ^= #40;
	}
$d2f4:
	a = x & #80;
	if (a != 0) { //beq $d307
		if (($20 & #80) != 0) { //beq d301
			$24++;
		}
		$21 ^= #80;
	}
$d307:
	return;
}
```





